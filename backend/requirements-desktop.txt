# Dependências do PROGRAMA INSTALADO (.exe) — e do build dele.
#
# Diferente de `requirements.txt` (que é o modo servidor: PostgreSQL + Redis +
# Celery), aqui está só o que roda no computador do contador:
#
#   - as dependências normais do backend;
#   - PyInstaller, que monta o .exe (só existe na máquina/runner do build);
#   - pystray + Pillow, que desenham o ícone ao lado do relógio.
#
# O `celery`, o `redis` e o `psycopg2-binary` de `requirements.txt` continuam
# instalados — o `.spec` os exclui do pacote. Assim o mesmo repositório atende
# aos dois modos com um ambiente só, e um bug de importação aparece no teste e
# não no computador do cliente.

-r requirements.txt

# --- empacotamento ---
pyinstaller==6.11.1

# --- ícone na bandeja do sistema ---
pystray==0.19.5
Pillow==10.4.0

# --- testes (o build no CI roda a suíte antes de publicar) ---
pytest==8.3.3
respx==0.21.1

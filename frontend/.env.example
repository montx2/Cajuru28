# URL pública da API vista pelo browser do usuário.
# Em Docker Compose local, o browser acessa a API em localhost:8000.
# Em preview remoto, use a URL pública da API (não localhost).
NEXT_PUBLIC_API_URL=http://localhost:8000
